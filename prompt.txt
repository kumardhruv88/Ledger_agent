Fix ONLY these two things in LandingContent.jsx:
1. Replace the vertical timeline (Section 6) with a horizontal 
   scrolling card carousel
2. Fix the footer completely
Do NOT touch any other section.

═══════════════════════════════════════════════════════════
SECTION 6 — AGENT CARDS (Horizontal Auto-Scroll Carousel)
═══════════════════════════════════════════════════════════

Replace the entire vertical timeline with this:

Outer section:
  background: white
  padding: 100px 0        ← no horizontal padding, cards bleed to edges
  width: 100%
  overflow: hidden

Header container:
  max-width: 780px
  margin: 0 auto 56px
  padding: 0 24px
  text-align: center

  Overline: "THE PIPELINE"
    font-size: 11px
    font-weight: 600
    letter-spacing: 0.14em
    color: #0d9488
    text-transform: uppercase
    display: block
    margin-bottom: 14px

  H2: "The 6-Stage Autonomous State Machine"
    font-size: clamp(26px, 3vw, 38px)
    font-weight: 700              ← NOT 800, thinner
    color: #0f172a
    letter-spacing: -0.03em
    line-height: 1.2
    margin: 0 0 12px 0

  Subtext:
    font-size: 15px
    font-weight: 400              ← thin
    color: #94a3b8
    line-height: 1.6
    letter-spacing: -0.01em

── CAROUSEL TRACK ──

Wrapper div:
  position: relative
  width: 100%

  Left fade overlay:
    position: absolute, left 0, top 0, bottom 0, width 80px
    background: linear-gradient(90deg, white, transparent)
    z-index: 2, pointer-events: none

  Right fade overlay:
    position: absolute, right 0, top 0, bottom 0, width 80px
    background: linear-gradient(270deg, white, transparent)
    z-index: 2, pointer-events: none

Scrolling track:
  display: flex
  gap: 20px
  padding: 20px 80px 32px
  overflow-x: auto
  scroll-behavior: smooth
  cursor: grab
  active: cursor: grabbing

  /* Hide scrollbar but keep scroll */
  scrollbar-width: none
  &::-webkit-scrollbar {{ display: none }}

  /* Auto-scroll animation */
  animation: autoScroll 30s linear infinite
  
  @keyframes autoScroll {{
    0%   {{ transform: translateX(0) }}
    100% {{ transform: translateX(-50%) }}
  }}
  
  Pause on hover:
    &:hover {{ animation-play-state: paused }}

  /* Duplicate the cards array to make seamless loop:
     render agents twice: [...agents, ...agents]
     so when it scrolls to the end it loops seamlessly */

── AGENT CARD ──

Each card:
  min-width: 260px
  max-width: 260px
  background: white
  border: 1px solid #e2e8f0
  border-radius: 18px
  padding: 24px 22px
  display: flex
  flex-direction: column
  gap: 0
  flex-shrink: 0
  position: relative
  overflow: hidden
  transition: all 280ms ease
  cursor: default

  Top accent bar:
    position: absolute
    top: 0, left: 0, right: 0
    height: 3px
    border-radius: 18px 18px 0 0

  Hover:
    transform: translateY(-8px)
    box-shadow: 0 20px 48px rgba(13,148,136,0.13),
                0 4px 12px rgba(0,0,0,0.06)
    border-color: #99f6e4

  STAGE NUMBER + BADGE ROW:
    display: flex
    align-items: center
    justify-content: space-between
    margin-bottom: 18px

    Stage number:
      width: 32px
      height: 32px
      border-radius: 8px
      display: flex
      align-items: center
      justify-content: center
      font-size: 13px
      font-weight: 700
      letter-spacing: -0.02em

    Nature badge:
      font-size: 9px
      font-weight: 700
      letter-spacing: 0.08em
      text-transform: uppercase
      padding: 3px 9px
      border-radius: 100px
      border: 1px solid

  AGENT NAME:
    font-size: 15px
    font-weight: 700          ← NOT 800, keep it sharp not heavy
    color: #0f172a
    letter-spacing: -0.03em
    line-height: 1.2
    margin-bottom: 8px

  DESCRIPTION:
    font-size: 12.5px
    font-weight: 400
    color: #64748b
    line-height: 1.6
    letter-spacing: -0.01em
    flex-grow: 1
    margin-bottom: 16px

  INPUT → OUTPUT:
    display: flex
    align-items: center
    gap: 6px
    padding: 8px 10px
    background: #f8fafc
    border-radius: 8px
    border: 1px solid #f1f5f9

    Input text:
      font-size: 10px
      font-weight: 500
      color: #94a3b8
      font-family: 'JetBrains Mono', monospace
      white-space: nowrap
      overflow: hidden
      text-overflow: ellipsis
      max-width: 85px

    Arrow "→":
      font-size: 11px
      color: #0d9488
      flex-shrink: 0
      font-weight: 700

    Output text:
      font-size: 10px
      font-weight: 600
      color: #0d9488
      font-family: 'JetBrains Mono', monospace
      white-space: nowrap
      overflow: hidden
      text-overflow: ellipsis
      max-width: 95px

Cards data with accent colors:

Card 0 — A0 Janitor:
  accent bar: #6366f1 (purple)
  stage number: bg #eef2ff, color #6366f1
  badge: bg #eef2ff, color #4338ca, border #c7d2fe, text "DETERMINISTIC"
  name: "A0 Janitor"
  desc: "Type coercion, dedup, and rule-based domain annotation"
  input: "Raw CSV" → output: "cleaned_df"

Card 1 — A1 Profiler:
  accent bar: #0d9488 (teal)
  stage number: bg #f0fdfa, color #0d9488
  badge: bg #f0fdfa, color #0d9488, border #99f6e4, text "DETERMINISTIC"
  name: "A1 Profiler"
  desc: "Shapiro-Wilk, entropy, outlier detection. Zero LLM."
  input: "cleaned_df" → output: "profile_json"

Card 2 — A2 Proposer:
  accent bar: #8b5cf6 (violet)
  stage number: bg #ede9fe, color #7c3aed
  badge: bg #ede9fe, color #5b21b6, border #ddd6fe, text "LLM + RAG"
  name: "A2 Proposer"
  desc: "Reads profile, proposes 5-12 testable hypotheses via RAG"
  input: "profile_json" → output: "Hypotheses"

Card 3 — A3 Registrar:
  accent bar: #ef4444 (red)
  stage number: bg #fef2f2, color #ef4444
  badge: bg #fef2f2, color #991b1b, border #fecaca, text "🔒 FREEZE"
  name: "A3 Registrar"
  desc: "SHA-256 locks the registry. No new hypotheses after this point."
  input: "Hypotheses" → output: "Frozen ⟶ hash"

Card 4 — A4 Executor:
  accent bar: #f59e0b (amber)
  stage number: bg #fffbeb, color #d97706
  badge: bg #fff7ed, color #c2410c, border #fed7aa, text "LLM + REACT"
  name: "A4 Executor"
  desc: "Writes pandas code, sandboxed run, 3-attempt repair loop"
  input: "Registry" → output: "raw_data"

Card 5 — A5 Statistician:
  accent bar: #10b981 (green)
  stage number: bg #f0fdf4, color #10b981
  badge: bg #f0fdf4, color #166534, border #bbf7d0, text "DETERMINISTIC"
  name: "A5 Statistician"
  desc: "BH-FDR across full family, effect sizes, licensed_text"
  input: "raw_data" → output: "LedgerEntry"

── CONNECTING ARROWS between cards ──
Do NOT add visible arrows between cards in the carousel.
The flow is implied by the card order + auto-scroll.
Instead, add a subtle connector dot row below the cards:

  Below carousel, centered:
  display: flex, gap 8px, justify-content center, mt 8px

  6 dots connected by lines:
    Each dot: 8px circle
    Colors matching each agent's accent color
    Between dots: 1px line, color #e2e8f0, width 24px

  This gives a minimal "step indicator" feel without clutter.

═══════════════════════════════════════════════════════════
GLOBAL FONT SHARPNESS FIX
═══════════════════════════════════════════════════════════

Add these to the global CSS (index.css):

* {
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  text-rendering: optimizeLegibility;
}

For ALL headings (h1,h2,h3,h4) across the page:
  font-weight: 700    ← change from 800 to 700 everywhere
  letter-spacing: -0.03em

For ALL body text, labels, badges:
  font-weight: 400 or 500 (never 600+ for body)
  letter-spacing: -0.01em

For ALL overlines/badges:
  font-weight: 600 (not 700)
  letter-spacing: 0.10em

This makes everything feel sharp, thin, editorial — not heavy.

═══════════════════════════════════════════════════════════
FOOTER — Complete Rewrite
═══════════════════════════════════════════════════════════

Replace the entire footer with this:

Outer footer:
  background: #0f172a
  width: 100%
  padding: 0

── TOP FOOTER BAND ──
  padding: 56px 0 48px
  border-bottom: 1px solid rgba(255,255,255,0.06)

  Inner container:
    max-width: 1160px
    margin: 0 auto
    padding: 0 48px
    display: grid
    grid-template-columns: 2fr 1fr 1fr 1fr
    gap: 48px
    align-items: start

    On tablet (max-width 768px): grid-template-columns: 1fr 1fr
    On mobile (max-width 480px): grid-template-columns: 1fr

  COLUMN 1 — Brand:
    Logo row (flex, align-items center, gap 8px, mb 14px):
      Logo mark: 32px × 32px, border-radius 8px
        background: linear-gradient(135deg, #0d9488, #0f766e)
        display flex, align-items center, justify-content center
        white "L", font-size 15px, font-weight 800
      Name: "Ledger"
        color: white, font-size 18px, font-weight 700
        letter-spacing: -0.02em

    Tagline:
      "The analyst that refuses to hallucinate."
      color: rgba(255,255,255,0.45)
      font-size: 13px
      font-weight: 400
      line-height: 1.6
      margin-bottom: 20px
      max-width: 220px

    Built-by line:
      color: rgba(255,255,255,0.3)
      font-size: 12px
      line-height: 1.6
      "Built at NSUT · CSE · BTP 2023–27"
      next line: "Dhruv Kumar · Rahul · Garv Bahl"

  COLUMN 2 — Project:
    Column heading:
      color: rgba(255,255,255,0.35)
      font-size: 10px
      font-weight: 700
      letter-spacing: 0.12em
      text-transform: uppercase
      margin-bottom: 16px

    Links (each on its own line):
      color: rgba(255,255,255,0.6)
      font-size: 13px
      font-weight: 400
      line-height: 1
      margin-bottom: 12px
      display: block
      text-decoration: none
      cursor: pointer
      transition: color 150ms ease
      hover: color white

    Links: About · Research Paper · Evaluation Protocol · GitHub

  COLUMN 3 — Stack:
    Heading: "STACK"
    Links: FastAPI · LangGraph · Groq API · LangSmith · React + Vite · Supabase

  COLUMN 4 — Agents:
    Heading: "AGENTS"
    Links: A0 Janitor · A1 Profiler · A2 Proposer · A3 Registrar · 
           A4 Executor · A5 Statistician

── BOTTOM FOOTER BAR ──
  padding: 20px 48px
  max-width: 1160px
  margin: 0 auto
  display: flex
  align-items: center
  justify-content: space-between

  Left:
    "© 2026 Ledger. NSUT Department of Computer Science & Engineering."
    font-size: 12px
    color: rgba(255,255,255,0.25)
    font-weight: 400

  Right (flex, gap 20px):
    3 small links: "Privacy" · "Terms" · "Contact"
    font-size: 12px
    color: rgba(255,255,255,0.25)
    hover: color rgba(255,255,255,0.6)
    transition: 150ms ease

  On mobile: flex-direction column, gap 8px, text-align center

═══════════════════════════════════════════════════════════
CRITICAL RULES
═══════════════════════════════════════════════════════════

1. Carousel auto-scrolls left continuously, pauses on hover
2. Duplicate the 6 cards to [...agents, ...agents] for seamless loop
3. Cards are 260px wide — fits ~4.5 visible on desktop, implying scroll
4. NO vertical timeline at all — delete it completely
5. Footer is 4-column grid — NOT raw stacked links
6. Footer links are styled opacity elements, NOT raw blue href anchors
7. Font globally: -webkit-font-smoothing antialiased everywhere
8. All heading font-weight 700 max, body 400-500 max
9. Footer bottom bar separates copyright (left) from links (right)
10. No framer-motion needed on the carousel — CSS animation only
    framer-motion only on the header text (fade in from below)