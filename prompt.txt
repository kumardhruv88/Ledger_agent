Fix ONLY these three sections in LandingContent.jsx:
- Section 4: Problem Statement + Stats counters
- Section 5: Feature Cards grid  
- Section 6: Agent Pipeline Timeline

Do NOT touch Navbar, Hero, CTA banner, or Footer.

═══════════════════════════════════════════════════════════
SECTION 4 — PROBLEM STATEMENT
═══════════════════════════════════════════════════════════

Outer section:
  background: #ffffff
  padding: 120px 24px
  width: 100%

Inner container:
  max-width: 780px
  margin: 0 auto
  display: flex
  flex-direction: column
  align-items: center
  text-align: center

── OVERLINE ──
  text: "THE PROBLEM"
  font-size: 11px
  font-weight: 700
  letter-spacing: 0.14em
  color: #0d9488
  text-transform: uppercase
  margin-bottom: 20px
  display: block

── HEADLINE ──
  Two separate lines, NOT one string:

  Line 1 — <h2> tag:
    text: "The Multiple Comparisons Problem."
    font-size: clamp(36px, 4.5vw, 56px)
    font-weight: 800
    color: #0f172a
    letter-spacing: -0.03em
    line-height: 1.1
    margin: 0
    display: block

  Line 2 — separate <span> or <p> below h2:
    text: "Solved."
    font-size: clamp(36px, 4.5vw, 56px)
    font-weight: 800
    color: #0d9488
    font-style: italic
    letter-spacing: -0.03em
    line-height: 1.1
    margin-top: 4px
    display: block

── BODY TEXT ──
  margin-top: 28px
  max-width: 620px
  font-size: 17px
  color: #64748b
  line-height: 1.8
  font-weight: 400
  letter-spacing: -0.01em
  text-align: center

  Text:
  "LLMs are eager to please. When handed a dataset and asked for insights,
  they will test thousands of combinations and only report the statistically
  significant ones — guaranteeing false discoveries. Ledger eliminates
  p-hacking by explicitly separating hypothesis generation from testing,
  enforced structurally by the pipeline architecture."

── DIVIDER ──
  margin-top: 56px
  margin-bottom: 56px
  width: 64px
  height: 2px
  background: linear-gradient(90deg, #0d9488, #99f6e4)
  border-radius: 2px
  align-self: center

── STATS ROW ──
  display: grid
  grid-template-columns: repeat(3, 1fr)
  gap: 0
  width: 100%
  max-width: 640px
  margin: 0 auto
  border: 1px solid #e2e8f0
  border-radius: 16px
  overflow: hidden
  background: white

  Each stat cell:
    padding: 32px 24px
    display: flex
    flex-direction: column
    align-items: center
    text-align: center
    border-right: 1px solid #e2e8f0
    last-child: border-right: none
    transition: background 200ms ease
    hover: background #f0fdfa

    BIG NUMBER:
      font-size: 52px
      font-weight: 800
      color: #0d9488
      letter-spacing: -0.04em
      line-height: 1
      margin-bottom: 10px
      font-variant-numeric: tabular-nums

    LABEL:
      font-size: 12px
      font-weight: 500
      color: #64748b
      line-height: 1.5
      max-width: 120px
      text-align: center

  Stat 1: number "~14" / label "False findings by LLM agents on pure noise"
  Stat 2: number "0"   / label "False findings by Ledger (NULLSET target)"
  Stat 3: number "1.00"/ label "Groundedness — every claim auditable"

  Stat 2 number color: #ef4444 (red — zero is the goal, make it stand out)
  Stat 3 number color: #10b981 (green — perfect score)

  framer-motion: useInView on the stats row.
  When enters viewport:
    Each number animates from 0 to target over 1.8s
    Use useEffect + setInterval counting up
    Stagger each counter by 200ms

═══════════════════════════════════════════════════════════
SECTION 5 — FEATURE CARDS
═══════════════════════════════════════════════════════════

Outer section:
  background: #f8fafc
  padding: 120px 24px
  width: 100%

Inner container:
  max-width: 1160px
  margin: 0 auto

── HEADER ──
  text-align: center
  margin-bottom: 64px

  Overline: "HOW IT WORKS"
    font-size: 11px, font-weight 700, letter-spacing 0.14em
    color #0d9488, uppercase, display block, margin-bottom 16px

  H2: "Six Agents. One Auditable Truth."
    font-size: clamp(32px, 4vw, 48px)
    font-weight: 800
    color: #0f172a
    letter-spacing: -0.03em
    line-height: 1.15
    margin: 0

── CARDS GRID ──
  display: grid
  grid-template-columns: repeat(3, 1fr)   ← STRICT 3 columns
  gap: 24px
  width: 100%

  On tablet (max-width 900px): grid-template-columns: repeat(2, 1fr)
  On mobile (max-width 600px): grid-template-columns: 1fr

Each card:
  background: white
  border: 1px solid #e2e8f0
  border-radius: 20px
  padding: 32px 28px
  display: flex
  flex-direction: column
  align-items: flex-start    ← LEFT aligned content, NOT centered
  text-align: left
  cursor: default
  transition: all 300ms ease
  position: relative
  overflow: hidden

  ::before (top gradient accent line):
    content: ''
    position: absolute
    top: 0, left: 0, right: 0
    height: 3px
    background: linear-gradient(90deg, #0d9488, #99f6e4)
    opacity: 0
    transition: opacity 300ms ease

  hover:
    transform: translateY(-6px)
    border-color: #99f6e4
    box-shadow: 0 20px 48px rgba(13,148,136,0.1),
                0 4px 12px rgba(13,148,136,0.06)
    ::before opacity: 1

── CARD ICON ──
  width: 48px, height: 48px
  background: #f0fdfa
  border-radius: 12px
  display: flex
  align-items: center
  justify-content: center
  margin-bottom: 20px
  border: 1px solid #ccfbf1
  transition: all 300ms ease

  Icon from lucide-react, size 22px, color #0d9488

  On card hover: icon container background #0d9488, icon color white

── CARD TITLE ──
  font-size: 16px
  font-weight: 700
  color: #0f172a
  letter-spacing: -0.02em
  margin-bottom: 10px
  line-height: 1.3

── CARD BODY ──
  font-size: 14px
  color: #64748b
  line-height: 1.7
  font-weight: 400
  flex-grow: 1           ← pushes badge to bottom

── CARD BADGE ──
  margin-top: 20px
  display: inline-flex
  align-items: center
  background: #f0fdfa
  border: 1px solid #99f6e4
  border-radius: 100px
  padding: 4px 12px
  font-size: 10px
  font-weight: 700
  color: #0d9488
  letter-spacing: 0.08em
  text-transform: uppercase

Cards data (6 total, fills 3x2 grid perfectly):

Card 1:
  Icon: Lock
  Title: "Hypothesis Pre-registration"
  Body: "Hypotheses are frozen before any test executes.
         Post-hoc selection is structurally impossible —
         not just discouraged."
  Badge: "A3 REGISTRAR"

Card 2:
  Icon: BarChart2
  Title: "Benjamini–Hochberg FDR Control"
  Body: "Every test is corrected across the full session
         family. The system refuses to report a finding
         it cannot statistically support."
  Badge: "A5 STATISTICIAN"

Card 3:
  Icon: FileSearch
  Title: "Claim-Level Provenance"
  Body: "Every sentence in the report links to the exact
         code that ran, the values returned, and the test
         that licensed it. Unbacked narration is rejected."
  Badge: "A6 REPORTER"

Card 4:
  Icon: Shield
  Title: "Sandboxed Code Execution"
  Body: "LLM-generated pandas code runs in an isolated
         subprocess with import blocklist, resource limits,
         and zero socket access."
  Badge: "A4 EXECUTOR"

Card 5:
  Icon: RefreshCw
  Title: "Self-Improving Loop"
  Body: "A8 Meta-Agent mines LangSmith telemetry for
         failure patterns and generates prompt patches —
         improving accuracy without retraining weights."
  Badge: "A8 META-AGENT"

Card 6:
  Icon: Database
  Title: "DPDPA-Compliant Local Analysis"
  Body: "No data leaves your machine. Run on clinical,
         financial, and student data that cannot legally
         be uploaded to cloud services."
  Badge: "LOCAL FIRST"

framer-motion on cards:
  useInView on the grid container
  When enters viewport: staggerChildren 0.08s
  Each card: initial {{opacity:0, y:32}} animate {{opacity:1, y:0}}
  transition: {{duration:0.45, ease:"easeOut"}}

═══════════════════════════════════════════════════════════
SECTION 6 — AGENT PIPELINE TIMELINE
═══════════════════════════════════════════════════════════

Outer section:
  background: white
  padding: 120px 24px
  width: 100%

Inner container:
  max-width: 780px
  margin: 0 auto

── HEADER ──
  text-align: center
  margin-bottom: 72px

  Overline: "THE PIPELINE"
    same style as other overlines

  H2: "The 6-Stage Autonomous State Machine"
    font-size: clamp(28px, 3.5vw, 44px)
    font-weight: 800
    color: #0f172a
    letter-spacing: -0.03em

  Subtext:
    "A rigid, inspectable LangGraph pipeline where every
     transition is a correctness property — not a suggestion."
    font-size: 16px, color #64748b, margin-top 12px, line-height 1.7

── TIMELINE LAYOUT ──
  position: relative
  display: flex
  flex-direction: column
  gap: 0

  CENTER VERTICAL LINE:
    position: absolute
    left: 28px            ← aligns with center of number circles
    top: 0
    bottom: 0
    width: 2px
    background: linear-gradient(180deg, #0d9488 0%, #ccfbf1 100%)
    z-index: 0

Each timeline row:
  display: flex
  align-items: flex-start
  gap: 24px
  position: relative
  padding-bottom: 40px
  last-child: padding-bottom 0

  LEFT — Number circle:
    width: 56px
    height: 56px
    min-width: 56px        ← prevents shrinking
    border-radius: 50%
    background: #0d9488
    color: white
    font-size: 18px
    font-weight: 800
    display: flex
    align-items: center
    justify-content: center
    position: relative
    z-index: 1
    border: 3px solid white
    box-shadow: 0 0 0 2px #0d9488, 0 4px 12px rgba(13,148,136,0.3)

    Special for A3 Registrar:
      background: #ef4444
      box-shadow: 0 0 0 2px #ef4444, 0 4px 12px rgba(239,68,68,0.3)

  RIGHT — Content card:
    flex: 1
    background: white
    border: 1px solid #e2e8f0
    border-radius: 16px
    padding: 24px 28px
    transition: all 250ms ease
    hover:
      border-color: #99f6e4
      box-shadow: 0 8px 24px rgba(13,148,136,0.08)
      transform: translateX(4px)

    TOP ROW (flex, align-items center, gap 10px, margin-bottom 10px):
      Agent name:
        font-size: 17px
        font-weight: 700
        color: #0f172a
        letter-spacing: -0.02em

      Nature badge:
        display: inline-flex
        align-items: center
        border-radius: 100px
        padding: 3px 10px
        font-size: 10px
        font-weight: 700
        letter-spacing: 0.07em
        text-transform: uppercase

        DETERMINISTIC → bg #f0fdfa, color #0d9488, border 1px solid #99f6e4
        LLM           → bg #fffbeb, color #92400e, border 1px solid #fde68a
        LLM + RAG     → bg #ede9fe, color #5b21b6, border 1px solid #ddd6fe
        LLM + REACT   → bg #fff7ed, color #c2410c, border 1px solid #fed7aa

    DESCRIPTION:
      font-size: 14px
      color: #64748b
      margin-bottom: 14px
      line-height: 1.6

    INPUT → OUTPUT ROW:
      display: flex
      align-items: center
      gap: 10px
      padding: 10px 14px
      background: #f8fafc
      border-radius: 10px
      border: 1px solid #e2e8f0
      font-size: 12px
      font-family: 'JetBrains Mono', 'Fira Code', 'Courier New', monospace

      INPUT chip:
        background: white
        border: 1px solid #e2e8f0
        border-radius: 6px
        padding: 3px 10px
        color: #475569
        font-weight: 500
        white-space: nowrap

      Arrow: →
        color: #0d9488
        font-weight: 700
        font-size: 14px
        flex-shrink: 0

      OUTPUT chip:
        background: #f0fdfa
        border: 1px solid #99f6e4
        border-radius: 6px
        padding: 3px 10px
        color: #0d9488
        font-weight: 600
        white-space: nowrap

Timeline stages data:

Stage 0: A0 Janitor | DETERMINISTIC
  desc: "Type coercion, dedup, domain annotation (medical/financial/temporal)"
  input: "Raw CSV bytes"
  output: "cleaned_df + ColumnProfiles"

Stage 1: A1 Profiler | DETERMINISTIC
  desc: "Deep statistical summary: Shapiro-Wilk, Shannon entropy, outliers"
  input: "cleaned_df"
  output: "profile_json (8k chars)"

Stage 2: A2 Proposer | LLM + RAG
  desc: "Reads profile + RAG context, proposes 5-12 strictly testable hypotheses"
  input: "profile_json + RAG"
  output: "HypothesisEntry list"

Stage 3: A3 Registrar 🔒 | DETERMINISTIC
  desc: "SHA-256 freeze — registry is immutable after this. BH denominator locked."
  input: "Hypothesis list"
  output: "Frozen Registry + hash"
  circle color: red (#ef4444)

Stage 4: A4 Executor | LLM + REACT
  desc: "Writes pandas code, executes in sandbox, 3-attempt repair loop per hypothesis"
  input: "Frozen Registry + schema"
  output: "raw_data per hypothesis"

Stage 5: A5 Statistician | DETERMINISTIC
  desc: "Assumption checks, test selection, BH-FDR across full family, effect sizes"
  input: "raw_data"
  output: "LedgerEntry (SUPPORTED / NOT-SUPPORTED)"

framer-motion on timeline:
  Each row: initial {{opacity:0, x:-20}} animate {{opacity:1, x:0}}
  Stagger: 0.12s between each row
  Trigger: useInView on the timeline container
  The vertical line: initial scaleY:0 → animate scaleY:1
    transition: duration 1.2s, ease "easeOut"
    transform-origin: top

═══════════════════════════════════════════════════════════
CRITICAL RULES
═══════════════════════════════════════════════════════════

1. Feature cards MUST be 3 columns — use CSS Grid, not flexbox
2. Timeline number circles MUST be fixed 56px — no shrinking
3. All section max-widths enforced — nothing bleeds full screen width
4. Stats row MUST be a 3-column grid with borders between cells
5. Every card title LEFT aligned (not centered like the old version)
6. No full-width buttons anywhere in these sections
7. Input/output row uses monospace font for the chip text
8. Do not add any dark backgrounds
9. Responsive: cards go 2-col at 900px, 1-col at 600px
          timeline stays single column at all sizes
10. Use useInView from framer-motion for all scroll animations:
    import {{ useInView }} from 'framer-motion'
    const ref = useRef(null)
    const isInView = useInView(ref, {{ once: true, margin: "-100px" }})